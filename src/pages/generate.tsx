import { useQuery } from "@apollo/client";
import * as GraphQLQueries from '../graphql/queries'; 
import Base from '../template/Base';
import GenerateTemplate from '../template/Generate';

const Home = () => {
    return <GenerateTemplate />

}

export default Home;
